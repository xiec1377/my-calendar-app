### Calendar app
This app was built with `Next.js` with Tailwind & shadcn UX components and internal Prisma PostgreSQL DB.

`npm run dev`
`npx prisma dev`
`npx prisma studio`
### Calendar app
Just a simple calendar app that allows you to create, edit and delete events in monthly, weekly and daily view. You can also filter events based on calendar.

This app was built with `Next.js` framework (`React` + `Node.js`) using Tailwind CSS and shadcn/ui for the frontend, and Prisma with PostgreSQL for the backend.


To start server:
```
npm run dev
```

To regenerate Prisma client for database:
```
npx prisma dev
```

To open local Prisma web server to view database:
```
npx prisma studio
```
